
//using props to pass down the selection of the api and displaying it on the website.
const DisplayInfo = (props) => {
    return (
        <section>

            <h2>{props.citySelection}</h2>                                                                                                         
        </section>
    )
}

export default DisplayInfo;